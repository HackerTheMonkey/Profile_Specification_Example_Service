This is an example service that depicts how to create a profile specification with a CMP interface and a profile abstract class that implement all the methods that are included inside the profile local interface and the CMP interface. The returned object after creating a profile using the profile table is basically a profile local object that can be type casted into the developer-defined profile local interface.